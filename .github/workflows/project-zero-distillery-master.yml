name: Project Zero Sensory Master Validation

on:
  push:
    branches:
      - feature/whisky-sensory-governance
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate-and-build-sensory:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source branch
        uses: actions/checkout@v4
        with:
          ref: feature/whisky-sensory-governance
          fetch-depth: 0

      - name: Fetch authoritative manifest from whisky-bar
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p .build-input
          status=$(curl -sS -o /tmp/manifest-response.json -w '%{http_code}' \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/CASK23/cask23-whisky-bar/contents/project-zero/governance/pz-whisky-sensory-completeness.manifest.json?ref=main")
          if [ "$status" != "200" ]; then
            echo "Manifest fetch failed with HTTP $status"
            cat /tmp/manifest-response.json
            exit 21
          fi
          python - <<'PY'
          import json, base64, pathlib
          obj=json.load(open('/tmp/manifest-response.json','r',encoding='utf-8'))
          pathlib.Path('.build-input/pz-whisky-sensory-completeness.manifest.json').write_bytes(base64.b64decode(obj['content']))
          PY

      - name: Build deterministic sensory master
        run: |
          set -euo pipefail
          python scripts/build_project_zero_sensory_master.py \
            .build-input/pz-whisky-sensory-completeness.manifest.json \
            --progress-dir data/sensory-research \
            --output PROJECT_ZERO_SENSORY_MASTER.json | tee sensory-build-metrics.json

      - name: Validate hard gates
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path
          data=json.loads(Path('PROJECT_ZERO_SENSORY_MASTER.json').read_text(encoding='utf-8'))
          records=data['records']
          assert data['recordCount']==262
          assert len(records)==262
          ids=[r['beverageId'] for r in records]
          assert len(set(ids))==262
          allowed={'orchard-fruit','citrus','dried-fruit','tropical','sweet','chocolate-coffee','spice','nutty','oak','floral-herbal','maritime','smoke-peat'}
          urls=set(); statuses={}
          for r in records:
              desc=r.get('descriptors') or []
              fm=r.get('descriptorFamilies') or {}
              assert set(fm)==set(desc), f"family keys mismatch {r['beverageId']}"
              assert all(v in allowed for v in fm.values()), f"invalid family {r['beverageId']}"
              statuses[r['governanceStatus']]=statuses.get(r['governanceStatus'],0)+1
              for src in r.get('sources') or []:
                  if isinstance(src,str) and src.startswith(('http://','https://')): urls.add(src)
          report={'version':data.get('version'),'recordCount':262,'uniqueBeverageIds':262,**data['metrics'],'distinctExternalSourceUrls':len(urls),'governanceStatuses':statuses,'allowedVisualFamilies':len(allowed),'validation':'PASS'}
          Path('PROJECT_ZERO_SENSORY_MASTER.validation.json').write_text(json.dumps(report,indent=2)+'\n',encoding='utf-8')
          print(json.dumps(report,indent=2))
          PY

      - name: Commit validated master payload
        run: |
          set -euo pipefail
          git config user.name "CASK23 Project Zero Bot"
          git config user.email "nilesh@cask23.co.uk"
          git add PROJECT_ZERO_SENSORY_MASTER.json PROJECT_ZERO_SENSORY_MASTER.validation.json
          if git diff --cached --quiet; then
            echo "Master payload already current"
          else
            git commit -m "build: compile validated Project Zero sensory master"
            git pull --rebase origin feature/whisky-sensory-governance
            git push origin HEAD:feature/whisky-sensory-governance
          fi
